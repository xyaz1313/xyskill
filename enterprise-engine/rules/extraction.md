# 抽取规则（G1 定型，之后每个批次只追加不改写）

## 什么算一条原子
{一个判断一条；案例不拆；每条脱离上下文能读懂。客户领域里"一个判断"的具体形态写在这里，例如代码库：一个模块的职责陈述 / 一条不变量 / 一次事故的因果。}

## type 判定口径
{8 个 type 在本领域的对应物。拿不准看谓语："是什么"→definition，"必须/应该"→principle，"怎么做"→method，"有一次"→case，"别这么干"→anti-pattern。}

## 脱敏
{人名 / 客户名 / 系统名的替换规则。来源方名单在 profile.blocklist_file。}

## 判例（G1 三篇样本里人改过的地方，改法写成规则）
| # | 原抽法 | 改成 | 规则 |
|---|---|---|---|

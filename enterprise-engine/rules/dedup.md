# 去重与层级规则（G4）

原则：related ≠ same，宁可留重复不错合并。两个节点标题相近，先问"证据集合是否基本重合"，再问"一个是不是另一个的细化"；都不是就各留各的。

三种结论：`merge`（证据集合基本重合且摘要可互换）/ `refines`（一个是另一个在某条件下的细化，保留两个节点建关系）/ `keep_separate`。

## 判例
| # | A | B | 结论 | 依据 |
|---|---|---|---|---|

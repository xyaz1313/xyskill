# 批次数据质量规则（G2，每批次一节，追加式）

原则：上游给的比例、前缀、来源特征只是先验，每个批次独立抽样 20–30 条再定规则；上一批次的规则不能直接套到下一批次。

判断标准的写法：不写"看元数据像不像"，写"内容的核心机制依赖什么、话题词是不是句末硬贴上去的"。

## 批次 {标识} · {日期}
- 输入：{条数、来源分布}
- 先验：{上游估计}
- 实测：{抽样结果，比例}
- 规则：{一句可执行的判断标准 + 正例 id + 反例 id}
- 保留的例外：{同批次里逐条核实后保留的 id，它们是下一次回归的最难负例}

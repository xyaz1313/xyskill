# 闸门包 · {闸门编号 G0–G6} · {工程名} · {批次}

> 一个闸门包只回答一件事：这一步可以过吗，过的话规则定成什么样。
> 审的人只看 §3 和 §4；其余部分是让 §3 §4 站得住的材料。控制在一屏到两屏之内，装不下就拆批次，不要加长。

## 0. 头

| 项 | 值 |
|---|---|
| 闸门 | G{n} {闸门名} |
| 工程 / 批次 | {客户工作区名} / {批次标识} |
| 生成时间 · 生成者 | {时间} · {模型或人} |
| 上游闸门包 | {上一个闸门包文件名；G0 写"无"} |
| 输入版本 | {atoms.jsonl 行数 + 最后一条 id；代码库则写快照标识与日期} |
| 预计成本 | {本闸门到下一闸门之间的 token 估算，按"每千条原子 3–7 万 token"粗估} |

## 1. 输入摘要（只放计数，不放正文）

{本步处理了什么：数量、分布（按 type / 来源前缀 / topic）、与上一版的差异。表格，不写句子。}

## 2. 抽样（脱敏后可外发的部分）

{由跑闸门的人挑选的少量样本，编号，每条不超过三行。审核档/涉密客户：这一节由客户自己决定放什么，模板不强制数量。}

## 3. 规则提案

{本闸门要定下来的规则，写成能直接追加进 `rules/*.md` 的形式。每条规则带：适用范围、判断标准、一个正例一个反例。}

**先验 vs 实测**：{如果这一步是在核实某个上游给的估计（比如"约 44% 是错标"），这里并排写：上游估计 / 本次实测 / 差异原因。核实前不要采信上游数字。}

## 4. 待拍板

| # | 问题 | 选项 | 模型建议 | 默认（无人回应时） |
|---|---|---|---|---|
| 1 | {一句话} | A / B / C | {选哪个，一句理由} | {保守选项} |

规则：合并、拆分、`contradicts`、摘要重写，默认永远是"不做"；只有人明确选了才做。

## 5. 校验

{`ontology-lint.py` 结果（违规数）；本闸门相关的回归用例一致率（`regression/run.py score`）；没跑就写"未跑"，不要空着。}

## 6. 决定记录（审的人填）

| # | 决定 | 理由 | 回写到 |
|---|---|---|---|
| 1 | 采纳 / 修改为… / 驳回 | {一句} | rules/{文件}.md §{节} |

**接手方推翻**：{如果审的人或下一步执行者推翻了上游闸门包的结论（比如换掉一组配对），在这里记：推翻了什么、依据、原结论错在哪。这一栏存在的目的是让推翻成为常规动作，不是事故。}

## 7. 范围内但跳过

{本批次里判断为"量太小 / 边界模糊 / 不值得建节点"而明确跳过的部分，写清楚跳过理由和"什么条件下回头再看"。没有就写"无"。}

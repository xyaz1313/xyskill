# 收官通盘审查报告（2026-09-18）

> 审查对象：`skills/xy-ontology/`、`enterprise-engine/`、`regression/`、204 条 `XY-ONT-` 原子、309 个概念节点，当一个整体看。
> 方法：不是读代码"看起来对"，是编了一个假客户工作区（自定义 topics / 自定义 id 前缀 / 10 条假原子 / 2 个假节点 / 一份来源名单 / 故意塞的死链和真名），把引擎从 lint → 检索 → 台账 → 合并器 → 人闸落盘 → 再 lint 完整跑了两遍（第一遍暴露问题，修完第二遍验证）；回归套件三个子命令用满分卷、随机卷、部分卷、空卷、非法标签、未知 id 各打了一遍；再用脚本对 309 节点和 204 原子做结构核对。
> 严重度：**C** 实测跑不通 / 数据错 · **H** 用户会撞上 · **M** 不一致或口径漂移 · **L** 小

## 1. 问题清单

### 实测跑不通的（全部已修）

| # | 严重度 | 问题 | 实测现象 | 处理 |
|---|---|---|---|---|
| 1 | C | `consolidate.py` 同一秒内两次运行用同一个文件名 | 第一次出 7 条提案，第二次 `--force` 出 0 条并**覆盖**了第一次的文件；`apply` 拿到空文件，整条流程静默失败 | 文件名加运行序号 `-rNNN`；台账无新记录时明确提示；新增 `--replay` 可重出提案而不推进合并点 |
| 2 | C | 企业布局（`ontology/profile.json`）下 lint 默认找不到画像 | 静默退回 XY 内置 topics，对客户 10 条原子报 14 条假违规 | 画像查找顺序改为：显式 > atoms 同目录 > `<工作区>/ontology/profile.json`；企业版 `atoms-search.py` 同样修 |
| 3 | C | 免费 skill 的检索命令默认先打云端 | 云端库没有"本体论与FDE方法论"话题，带 `skill` 字段查询返回 `{"atoms":[]}` **且不回退本地**；现在能拿到结果纯粹因为 SKILL.md 的命令没带 `--skill`，云端 422 报错才触发回退——换个写法就断 | SKILL.md 命令改为显式 `--file <本 skill 目录>/references/atoms.jsonl`，并写明原因；**云端同步 204 条原子这件事仍待有权限的一方做** |
| 4 | H | `apply.py` 不幂等 | 同一份决定再 apply 一次，`summary_stale` 再改一次、版本号再 +1、判例翻倍 | 判例记 `packet` 字段，同一提案包同一提案只处理一次；`replay` 出的新包仍会处理（有意：重出就是要重审） |
| 5 | H | `apply.py` 对 decisions 里不存在的提案 id 静默忽略 | 写错 id 什么都不发生、没有提示 | 加警告 |
| 6 | M | `regression/run.py score` 对空卷（answer 全 null）输出 0/0 = 0.0% 且退出码 0 | 会被当成"跑过了" | 无有效作答直接退出码 1 并提示；未知 id 加警告 |
| 7 | M | `consolidate.py` 覆盖缺口关键词切分漏四字词 | "会员积分"切不出"积分"，3 条同主题 query 聚不出缺口 | 已在上一轮发现并修（此处记档） |

### 各块之间说法打架（已修）

| # | 严重度 | 问题 | 处理 |
|---|---|---|---|
| 8 | H | **免费层讲"六步法"，引擎讲"闸门 G0–G6"，两套词没有对照表**——客户从 `xy-ontology` 走到引擎会迷路（任务书问题 1、2 的核心） | `enterprise-engine/README.md` 加"六步法 ↔ 闸门序列"对照表；`xy-ontology` 的诚实边界话术加一句"XY 有一套对着客户数据跑的引擎（六步法拆成一串闸门，每个闸门由人拍板）"，不展开、不报价 |
| 9 | H | `xy-atomize` 的 `validate_batch.py` 硬编码 13 个 topics，SKILL.md 也写"从 13 个里选" | 新抽的原子只要打"通用商业管理"或"本体论与FDE方法论"就过不了验收——lint 已改读 profile，入库验收还是老清单，**两道门口径不一** | `validate_batch.py` 改读 `knowledge/profile.json`（找不到才用内置 15 个）；SKILL.md 改为"以 profile.json 为准" |
| 10 | M | `ontology-schema.md` 说 topics 是 14 个，`profile.json` 是 15 个 | 改为 15 个并注明权威来源是 profile.json |
| 11 | M | README 中英文写 27,853 条原子、13 个方向打标签；实际 28,057 条、15 个话题 | 数字改正；"13 个方向"指板块图不指 topics，保留 |
| 12 | M | `enterprise-engine/README.md` 把回归套件说成 G-1 校准闸门，没说清它测的是什么 | 加"如实说明"：170 题来自 XY 私域库、11 题来自代码库实操；测的是模型做这类判断靠不靠谱，不是客户领域 |

### 数据层（记录，未动）

| # | 严重度 | 发现 | 建议 |
|---|---|---|---|
| 13 | H | **309 个节点里 182 个 summary 超过 schema 的 200 字上限**（>400 字 2 个、301–400 字 52 个、201–300 字 128 个，最长 415） | 这是 HANDOFF-2 在"覆盖 12 个 topic"与"逐条精修"之间取舍的结果。schema 已改为"≤200 建议、≤450 硬上限"并指向本报告；超长节点多是 COG/CONT/IP 里"哲学/心法"类大集群，值得二次细分。前 25 见文末附录 |
| 14 | H | **两个节点完全重复**：`CPT-TEAM-012` 与 `CPT-IP-004` 标题、摘要一字不差（"流量生意vs IP生意判断标准"） | 跨 topic 建节点时漏掉的重复。该合并（按"宁可留重复不错合并"原则至少要标 refines），是人的决定，未动 |
| 15 | M | 13 条证据原子的 topics 不含所在节点的 category（上轮记 7 条，HANDOFF-2 后 13 条） | 方法论允许跨 topic 归证据，但应在 `concepts-schema.md` 写成明文规则而不是"发现了记一笔" |
| 16 | M | **"本体论与FDE方法论"这个 topic 自己没有概念节点**（0 个），204 条原子里 201 条没有 `related` | 卖概念节点方法论的产品，自己的方法论话题没跑过流水线，跟同行 PK 时会被问。建议单独跑一轮阶段 0–5（204 条量很小） |
| 17 | M | `XY-ONT-054/056/057/060–064/076` 点名了 9 家国内公司（银行、车企、电池厂），来源章节是"21-解析-Ontology4篇到底是什么" | 若该章节是第三方博主的解析而非官方/学术材料，按三档内容政策应"重塑不点名"；请主会话核一下这章的来源档位 |
| 18 | L | `XY-ONT-` 编号跳段（1–101、686–761、804–831） | 分批蒸馏的痕迹，不影响使用；README 里"往库里加原子"没说编号规则，可补一句 |
| 19 | L | `atoms-search.py --skill X` 对**没有** `skills` 字段的原子不过滤（如 `XY-UZB-618` 会出现在 `--skill xy-ontology` 结果里） | 全仓库 31 份副本的既有行为，不是本轮引入；要改就得同步 31 份，建议下次动 atoms-search 时一起 |

### 通盘判断（回答任务书四问）

1. **连贯吗？** 修完 #8/#9 之后是连贯的：免费层讲"是什么"+六步法，引擎按闸门跑六步法，回归套件是开工前的校准，lint/atomize/引擎三处 topics 都以 `profile.json` 为准。修之前不连贯——两套词汇、两套 topics 清单。
2. **免费→付费的路顺吗？** 现在有一句话把人接过去（"XY 有一套引擎……找小爷"），够用但薄：没有任何东西告诉客户"你带什么材料来"。建议下一步在 `xy-ontology` 的"下一步"里加一条"想走引擎，先把这三样准备好：系统清单 / 一份最像样的流程文档 / 你们最常打架的两条规则"。
3. **引擎跑得通吗？** 修完 #1/#2/#4/#5 之后，在假工作区里 G0（lint 定边界）→ 检索 → G6（台账→合并→人闸→落盘→再 lint）**全程跑通**，同向复核确实把与既有证据 contradicts 的原子拦下了。但要说清楚：G1–G5 的"判断"本身仍是运行环境的模型在会话里做，脚本只校验、出题、记账——这是架构决定，不是没做完。
4. **regression 三个子命令都能用吗？** 能。`check` 167 题 0 漂移；`emit` 全量 181 题与按 kind 都能出；`score` 满分卷 100%、随机卷 45.4%（阈值拦下）、部分卷只算作答部分、空卷退出码 1、非法标签算错、未知 id 报警告；`build_cases.py` 重跑结果确定。

## 2. 缺口 1（个人作者 FDE 指南书）

不在本机权限范围，原始文件我看不到，**未处理，无增量**。审查角度只提一条：如果它进了任何原子，`ontology-lint.py --sources-dir` 现在能查 ≥24 字原文照搬；来源方名字进 `tools/name_blocklist.txt` 就能查点名。这两道机器检查是这轮新建的，处理缺口 1 时可以直接用。

## 3. 缺口 2（nano-ontoprompt 四层模型：缺不缺 Logic-Rule 层）

原始材料同样看不到，以下判断只基于仓库现状：

**缺，而且现在缺得对。** 仓库里的本体有 Class（8 type）、Relation（5 rel）、Action（5 种动作）三层；"逻辑/规则层"目前分散在三处：`ontology-lint.py` 的硬规则（枚举封闭、无死链、case/number 必须有来源）、`enterprise-engine/rules/*.md` 的判例文本、`concepts-schema.md` 里"principle 孤立是警号""contradicts 要先同场景"这类散文规则。**没有一层是机器可对图执行的推理规则**（比如"anti-pattern 必须有对立的 principle""同一 definition 术语不得有两条未标 contradicts 的定义"——schema 里写了，lint 没查）。

不建议现在补一个正式的 Logic-Rule 层，理由是两天里最硬的教训：规则一泛化就误伤（contradicts 自动推断撤回、两级漏斗失效、source_type 不能当改标依据）。正确顺序是 G6 判例先积累，规则从判例里长出来，再决定哪些能升级成 lint 硬规则。**可以马上做的一小步**：把 schema 里已经写成文字、lint 却没查的两条（anti-pattern 应有对立 principle / definition 唯一性）加成 lint 的软警告，不 fail，只列清单。

## 4. Critical 级修复清单（本轮已修并实测）

#1 提案文件覆盖、#2 画像找不到、#3 免费层检索靠报错回退、#4 apply 不幂等。修复后第二遍完整流程见上文第 3 问。

## 5. 我自己拍的板（可反驳）

- 182 个超长摘要没重写，只改了 schema 上限并列出清单——重写是判断活，一天写不完 182 条，硬写会降质。
- `CPT-TEAM-012`/`CPT-IP-004` 完全重复也没动——合并是人闸事项，脚本原则就是不代做。
- `replay` 出的新提案包会被 apply 再处理一次（幂等只在同一包内）——因为重出的目的就是重审，若想跨包幂等，判例键应改为提案内容哈希，我没做。
- 免费 skill 改用 `--file references/atoms.jsonl`，代价是单拷 skill 目录时查不到 `xy-fde` 的 UZB 原子——已在 SKILL.md 写明兜底说法。

## 附录：超长摘要前 25（全量 182 条可用 `python3 -c` 一行从 concepts.jsonl 过滤）

| # | 节点 | 标题 | 字数 |
|---|---|---|---|
| 1 | `CPT-COG-019` | 尼采式视域遗忘与专注开工原则 | 415 |
| 2 | `CPT-COG-028` | 创业决策公理集 | 402 |
| 3 | `CPT-CONT-010` | 内容创作哲学自查原则 | 397 |
| 4 | `CPT-IP-027` | IP与流量入口质量/交付平衡原则（防塌房） | 391 |
| 5 | `CPT-TRAF-011` | 获客型视频与心理锚点植入 | 382 |
| 6 | `CPT-COG-026` | 人生哲学与自我认知框架 | 373 |
| 7 | `CPT-COG-012` | 私域是做人的生意：信任、资源与关系积累认知 | 370 |
| 8 | `CPT-IP-032` | 信任背书构建模型（企业家IP/私域三维/身份三位一体） | 368 |
| 9 | `CPT-TRAF-006` | 打粉黑话体系 | 366 |
| 10 | `CPT-CASE-024` | 高客单价案例信任背书与企业家IP驱动 | 364 |
| 11 | `CPT-TRAF-014` | 直播间流量机制与节奏控制 | 361 |
| 12 | `CPT-COG-029` | 商业与内容分析的第一性原理心法 | 359 |
| 13 | `CPT-CONT-008` | 长内容切片方法论 | 358 |
| 14 | `CPT-COG-030` | 私域账号养护与执行层视觉秩序认知 | 357 |
| 15 | `CPT-IP-034` | IP矩阵规模化三阶段模型（心智位/品类位/生态位） | 350 |
| 16 | `CPT-COG-018` | 尼采式估价创造与自我立法原则 | 350 |
| 17 | `CPT-AIT-019` | 一人公司AI杠杆与竞争策略 | 347 |
| 18 | `CPT-AIT-011` | 自动化循环设计与AI工程四阶段演进 | 347 |
| 19 | `CPT-TEAM-029` | 一人公司与个人杠杆认知，AI冲击下的组织架构重塑 | 345 |
| 20 | `CPT-CONT-007` | 原创vs借鉴辩证：懂得借力的原创者 | 343 |
| 21 | `CPT-COG-005` | 认知与财富无必然联系，事本位而非人本位 | 343 |
| 22 | `CPT-CASE-022` | 技术/资源断层式优势原则（军事商业类比） | 342 |
| 23 | `CPT-CASE-021` | 尼采/福柯式复盘哲学原则 | 338 |
| 24 | `CPT-COG-009` | 悬浮人群与自媒体两种出发点 | 337 |
| 25 | `CPT-TRAF-018` | 内容素材迭代与账号推流模型 | 336 |

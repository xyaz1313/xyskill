# 付费引擎执行任务书 — 写给 Fable 5.1（第二轮，从设计到实做）

自包含，你不需要看过之前的对话。背景：你上一轮交的架构提案（`2026-09-18-paid-engine-architecture-proposal.md`，同目录）已经被XY主会话核实通过，用户确认接受第5.3节的定位收窄（付费引擎只做"内部知识/运营/合规"场景，不做获客/销售场景）。现在进入执行阶段——按你自己在提案第6节给的落地顺序，从第1步开始真正搭建。

## 现状（截至这份任务书写成时）

- XY自己的本体+概念节点体系：全部完成，`knowledge/atoms.jsonl`（28,057条原子，含新增的"本体论与FDE方法论"话题204条）+ `knowledge/concepts.jsonl`（309个概念节点，覆盖全部13个业务topic）
- 你的架构提案已确认：闸门式流水线（6个闸门G0-G5）+ 三档服务（自助/审核/驻场）+ 本地优先不联网的护城河设计
- 你自己标注的"必须先建的工件"：回归套件（第5.1节），目前不存在

## 请你按自己提案第6节的顺序，从第1步开始执行

### 第1步：回归套件（你自己判断这是"第一个要建的工件"，护城河也压在它上面）

去`~/fengwuzhishou`仓库里找这些真实数据作为标注样本来源：
- 私域运营topic试点的273条已确认匹配（原子↔概念节点，在`knowledge/concepts.jsonl`里能反查到，也可以看commit历史里两份HANDOFF文档的执行结果）
- G3矛盾候选的2个真实判例（`IPP-031`↔`XY-DY-053`、`XY-DY-190`↔`XY-DY-089`，判断依据记在`knowledge/concepts-schema.md`"真实案例"章节）
- 63.3%批次数据质量规则的判断依据（同样在`concepts-schema.md`和两份HANDOFF里）

从这些真实、已核实过的判断里，抽一组有代表性的用例，做成`regression/`目录（具体格式你自己设计，建议至少包含：输入原子+期望输出判断+判断类型如"该不该归入某节点/该不该标contradicts/该不该改topic"），再写一个跑这些用例的脚本，输出"当前环境在这组标注样本上的一致率"。

**这是你在自己提案里明确要求的"G-1校准闸门"的基础设施**——没有这个，后面所有闸门设计都是空中楼阁。

### 第2步：两处代码改造（你自己在提案4.2节点名的）

1. `scripts/ontology-lint.py`目前把14个（现在其实是15个，新增了"本体论与FDE方法论"）topics硬编码在`VALID_TOPICS`里，改成从`profile.json`读取，`atoms.jsonl`所在的XY自己库可以生成一份对应当前15个topics的`profile.json`作为默认档
2. `scripts/atoms-search.py`默认打云端`api.xyskill.xyz`，企业引擎版本必须改成默认本地、云端能力显式开启（不要改动XY自己在用的这份，企业版应该是一个独立的精简副本，具体怎么组织目录结构你自己判断——可以是同一个repo下的`enterprise-engine/`子目录，也可以是完全独立的包，选你觉得更好维护的方案）

### 第3步：闸门包格式定稿

设计一个markdown模板，G0-G5六个闸门共用同一套结构（你自己在提案里说的"闸门包"）。可以先看着你自己表格里列的每个闸门该装什么内容（边界声明草案/抽样/规则提案/待拍板问题）去定字段，不需要我们再确认，你有充分的设计自主权。

### 第4步：反向验证（你自己说这是"提案的自检"）

用XY自己已经建好的309个概念节点、`concepts-schema.md`记录的完整流水线历史，按你设计的六个闸门重新"回放"一遍——检查你的闸门包格式，装不装得下当时真实发生的全部决策过程。如果装不下，说明格式设计有缺陷，需要你自己迭代，不用回来问我们。

## 不要做的事

- 第5步（找第一个真实客户跑自助档）不在这次任务范围内，那是业务侧的事，不是你能推进的，跳过
- 不要重新论证已经拍板的架构决策（gated pipeline/三档服务/本地优先），除非你在实做过程中发现真正的硬伤
- 涉及具体客户数据、NDA保护内容的任何东西都不要往这个仓库里塞——这个仓库是公开的（`xyaz1313/xyskill`），今晚已经因为这个问题被拦截过两次，都是因为把没有脱敏/没有获得明确许可的内容往公开仓库推

## 完成后

commit + push回`https://github.com/xyaz1313/xyskill.git`，写清楚做了哪几步、遇到什么问题、哪些地方是你自己拍的板（沿用你上一轮"可以直接反驳我的点"这个写法，标注你做的哪些选择用户可能会不同意，方便我们快速核对）。

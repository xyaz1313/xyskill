# 独立复核清单 —— 交给 Fable 5.1

自包含文档。目的：这几天的G7/实时上下文/本体论收尾工作，一直是这边（主会话）自己验证自己改的东西，现在要一次真正独立的第二双眼睛核查——你不是来确认"我做的东西对不对"，是来找我们两边都可能漏掉的问题。发现问题直接改，不用先问。

## 背景：为什么现在要做这个

用户明确要求"没有任何问题了，是吗？"这个级别的确认，并且要发给内部小伙伴测试了。这份清单里列的每一项，主会话这边都已经查过、改过——但都是自己查自己，存在盲区（比如GitHub仓库自己的"About"简介字段，不在任何文件里，主会话这几天所有grep检查全部扫不到它，是打开真实网页才发现漏了）。你独立跑一遍，才是真正的第二次验证。

## 要查的范围

### 1. 两个仓库的"活文档"里还有没有过期数字

已知权威数字（截至2026-09-19）：46个skill、14个方向、27,966条知识原子、333个概念节点、私有仓库v0.1.0、公开仓库v2.9.0。

- 全文搜索`27853`、`26895`、`45个skill`、`45 skill`、`43个技能`、`43 skill`、`13个方向`、`13 areas`——公开仓库和私有仓库都要搜
- **不要改**`docs/superpowers/specs/`目录下的历史任务书/交接文档、`knowledge/concepts-schema.md`里已经标注"当时/去重后"的历史记录——这些是带时间戳的历史快照，不是活文档，出现旧数字是正常的，只要确认它们确实有清楚的历史标注、不会被误读成当前状态
- GitHub仓库本身的"About"简介字段（网页右侧栏，不在任何git文件里，要用`gh repo view --json description`查，或者直接打开网页看）——这个字段主会话这几天才发现要单独查，之前一直漏了

### 2. 两份README（中英文）互相对不对得上

`README.md`和`README.en.md`——检查英文版是不是完整跟上了中文版这几天加的东西（本体论/FDE那条线、第四张图、新增的表格行）。英文版之前有一整段"本体论与FDE怎么跑出来的"章节是完全缺失的，主会话刚补上，请核实补得对不对、有没有翻漏的地方。

### 3. 技能列表类文档，跟`skills/`目录实际内容对不对得上

`docs/功能说明书.md`、`docs/XY指令清单.md`、`docs/介绍-先读我.html`——逐个对照`ls skills/`的46个真实文件夹，看有没有漏列的、多列的、写错名字的。主会话这几天在`功能说明书.md`和`介绍-先读我.html`里各发现过一次真实遗漏（前者漏了`xy-ontology`，后者漏了`xy-ontology`/`xy-fde`/`xy-recut`三个），说明这类文档容易漏，请重新逐个核对一遍，不要假设主会话已经查干净了。

### 4. 知识原子的`related`关联完整性

`scripts/ontology-lint.py`（不带`--warn`）跑一遍，`死链数`必须是0——这个字段专门检查`related[].id`指向的原子是否真实存在，不存在就是死链。如果不是0，说明有原子引用了不存在的id，直接定位到具体是哪条、是什么时候引入的。

同时看一眼`--warn`模式下的软规则警告，不要求清零，但扫一眼有没有数量异常（比如警告数比上次记录的显著变多，可能是新引入的数据质量问题）。

### 5. 私有仓库（`xyskill-enterprise`）独立跑一遍验证套件

不要相信任何"应该没问题"的状态描述，实际执行：

```bash
python3 scripts/action-scaffold/test_scaffold.py
python3 scripts/action-scaffold/demo_fake_erp.py
python3 -W error::ResourceWarning scripts/action-scaffold/test_scaffold.py
python3 -W error::ResourceWarning scripts/action-scaffold/demo_fake_erp.py
```

前两条要通过（16测试OK，demo跑完整六步不报错）；后两条专门测有没有文件句柄未关闭的问题（`-W error`模式下有一个警告就会直接报错），应该完全跑通不报错——这是主会话这几天修过的一类问题，独立确认修干净了。

### 6. `docs/快速上手.md`里每条命令，独立重新执行一遍

不要读代码判断"看起来对"，实际在一个全新的临时目录里跑一遍文档里写的每一条命令（包括`init-workspace.sh`建练习工作区、抄文档给的示例原子、跑lint、跑检索、跑账本三条命令），确认文档描述的行为和实际输出一致。这份文档是给"第一次打开仓库的人"用的，一旦有一条命令实际报错而文档没说，新手会直接卡死。

### 7. 打包出去的zip压缩包内容对不对

用户要一份能直接发给小伙伴的压缩包，主会话已经打好放在`~/Desktop/xyskill-enterprise-v0.1.0.zip`，用的是`git archive`从私有仓库HEAD直接出，逐个文件跟仓库diff过、在全新临时目录解压后重新跑过一遍测试确认能独立工作。你没有这台机器的桌面访问权限，这条不用你验证，只是让你知道这件事已经做了、怎么做的，如果你有更好的打包方式的建议可以提。

## 交付

发现的每一处问题，属于"改了不会有争议"的（比如过期数字、明显的遗漏），直接改掉、commit、push；属于"要不要这样改需要判断"的，记下来但先不动，等主会话拿回来跟用户过一遍。完成后给一句话总结：查了几类问题、改了几处、还剩什么没定的。
